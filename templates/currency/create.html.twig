{% extends 'base.html.twig' %}

{% block title %}
	{{ page_title }}
{% endblock %}

{% block body %}

	<div class="display-grid">
		<nav aria-label="breadcrumb">
			<ol class="breadcrumb">
				<li class="breadcrumb-item">
					<a href="{{ path('app_home') }}">Нүүр хуудас</a>
				</li>

				<li class="breadcrumb-item">
					<a href="{{ path('app_currency_index') }}">Ханшийн мэдээлэл</a>
				</li>

				<li class="breadcrumb-item active" aria-current="page">
					Нэмэх
				</li>
			</ol>
		</nav>
	</div>
	<br>


	<div>
		<a href="{{ path('app_currency_download_example_file') }}" class="btn btn-primary btn-sm">
			жишээ татах</a>
	</div>

	<br>
	<br>


	{% include 'layout/flash.html.twig' %}

	{{ form_start(currencyForm) }}

	{# <div class="form-group">
			{{ form_row(currencyForm.base) }}
		</div> #}

	<div class="form-group">
		{{ form_row(currencyForm.rates) }}
	</div>

	<div class="form-group">
		{{ form_row(currencyForm.CurrencyDate) }}
	</div>

	{{ form_end(currencyForm) }}
{% endblock %}
