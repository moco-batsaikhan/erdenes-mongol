{% extends 'base.html.twig' %}

{% block title %}
	{{ page_title }}
{% endblock %}

{% block body %}

	<div class="card">
		<div class="card-body">
			<div class="display-grid">
				<nav aria-label="breadcrumb">
					<ol class="breadcrumb">
						<li class="breadcrumb-item">
							<a href="{{ path('app_home') }}">Нүүр хуудас</a>
						</li>

						<li class="breadcrumb-item">
							<a href="{{ path('app_news_index') }}">Мэдээ
							</a>
						</li>

						<li class="breadcrumb-item">
							Мэдээ PDF
						</li>
					</ol>
				</nav>
			</div>

			<div>
				<a href="{{ path('app_content_create_pdf') }}" class="btn btn-primary btn-sm">
					+ Шинээр нэмэх</a>
			</div>

			<br>

			{% include 'layout/flash.html.twig' %}

			<div class="table-responsive">
				<table class="table table-hover" id="dataTable" width="100%" cellspacing="0">
					<thead>
						<tr>
							<th>№</th>
							<th>Агуулгийн нэр</th>
							<th>Агуулгийн төрөл</th>
							<th>Төлөв</th>
							<th>Агуулгийн дараалал</th>
							<th>Мэдээ</th>
							<th>#</th>
						</tr>
					</thead>
					<tbody>
						{% for content in contents %}
							<tr>
								<td>{{ content.id }}</td>
								<td>{{ content.name }}</td>
								<td>{{ content.type }}</td>
								<td>
									{% if content.active == true %}
										<b style="color: darkgreen">Идэвхитэй</b>
									{% else %}
										<b style="color: red">Идэвхигүй</b>
									{% endif %}
								</td>
								<td>{{ content.priority}}</td>
								<td>
									<a href="{{ path('app_news_edit', {'id' : content.getNews.id }) }}" class="btn btn-primary btn-sm">
										{{ content.getNews.MnTitle }}
									</a>
								</td>
								<td>
									<a href="{{ path('app_content_pdf_edit', {'id' : content.id}) }}" class="btn btn-primary btn-sm">
										<i class="fas fa-edit"></i>
									</a>
								</td>
							</tr>
						{% endfor %}
					</tbody>

				</table>
			</div>
			{% if contents|length > 0 %}
				{% include 'layout/pagination.html.twig' %}
			{% endif %}
		</div>
	</div>
{% endblock %}
