{% extends 'base.html.twig' %}

{% block title %}
	{{ page_title }}
{% endblock %}

{% block body %}
	<head>
		<link href="https://fonts.googleapis.com/css?family=Roboto:300,300i,400,400i,500,500i,700,700i" rel="stylesheet">
		<link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
		<link href="{{ asset('sbadmin/css/draggable.css') }}" rel="stylesheet">
		{% block stylesheets %}{% endblock %}
	</head>


	<div class="card">
		<div class="card-body">
			<div class="display-grid">
				<nav aria-label="breadcrumb">
					<ol class="breadcrumb">
						<li class="breadcrumb-item">
							<a href="{{ path('app_home') }}">Нүүр хуудас</a>
						</li>

						<li class="breadcrumb-item">
							Нүүр хуудас дараалал
						</li>
					</ol>
				</nav>
			</div>

			<br>

			{% include 'layout/flash.html.twig' %}


			<div>
				<div class="page-content page-container" id="page-content">
					<div class="padding">
						<div class="row">
							<div class="col-sm-8">

								<div class="container-fluid d-flex justify-content-center">
									<div class="list list-row card" id="sortable" data-sortable-id="0" aria-dropeffect="move">
										{% for section in sections %}
											<div class="list-item" data-id="{{ section.id}}" data-item-sortable-id="0" draggable="true" role="option" aria-grabbed="false" style="">
												<div>
													<a
														href="#" data-abc="true">
														{# {% if section.code == 'ABOUT_US' %}
																																																																																																				<span class="w-40 avatar gd-primary">Б</span>
																																																																																																			{% else if section.code == 'GRAPIC' %}
																																																																																																				<span class="w-40 avatar gd-primary">Г</span>
																																																																																																			{% else if section.code == 'NEWS' %}
																																																																																																				<span class="w-40 avatar gd-primary">М</span>
																																																																																																			{% else if section.code == 'WORD' %}
																																																																																																				<span class="w-40 avatar gd-primary">У</span>
																																																																																																			{% else if section.code == 'VIDEO_NEWS' %}
																																																																																																				<span class="w-40 avatar gd-primary">В</span>
																																																																																																			{% else if section.code == 'JOB_ADS' %}
																																																																																																				<span class="w-40 avatar gd-primary">А</span>
																																																																																																			{% else if section.code == 'SUBSIDIARY_COMPANY' %}
																																																																																																				<span class="w-40 avatar gd-primary">О</span>
																																																																																																			{% else if section.code == 'PROJECT' %}
																																																																																																				<span class="w-40 avatar gd-primary">Т</span>
																																																																																																			{% endif %} #}
														<span class="w-40 avatar gd-primary">S</span>
													</a>
												</div>
												<div class="flex">
													<a href="#" class="item-author text-color" data-abc="true">
														{{ section.sectionName}}</a>
												</div>

											</div>
										{% endfor %}
									</div>
								</div>
							</div>
						</div>
					</div>
				</div>
			</div>
			<button type="submit" class="btn btn-primary">Хадгалах</button>
		</div>
	</div>
{% endblock %}
